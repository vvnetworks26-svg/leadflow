# LeadFlow Widget — Environment Variables
# Copy this file to .env and fill in your values.

# URL of the Express API server
VITE_API_URL=http://localhost:4000

# Organization slug or MongoDB _id — identifies which org the widget belongs to
VITE_WIDGET_TOKEN=your-org-slug
