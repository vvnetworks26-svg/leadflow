/**
 * Re-exports the shared widgetApiClient from @leadflow/shared.
 * Internal dashboard imports continue to use relative paths unchanged.
 */
export { widgetApiClient } from '@leadflow/shared/widgetApiClient';
export type { WidgetOrgConfig } from '@leadflow/shared/widgetApiClient';
