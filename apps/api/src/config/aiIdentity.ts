/**
 * aiIdentity.ts
 *
 * The AI receptionist's name is a fixed, product-wide brand — the same
 * across every client's widget (analogous to Intercom's "Fin"), not a
 * per-tenant setting. Centralised here as a named constant so it has one
 * discoverable source of truth, rather than a bare string literal repeated
 * wherever the identity is built.
 */

export const AI_ASSISTANT_NAME = 'Amara';
