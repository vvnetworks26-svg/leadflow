# Ensure optional native dependencies (Rollup platform binaries, esbuild, etc.)
# are always installed regardless of the current OS/CPU.
# This is required for Vercel (Linux x64) deployments when the lockfile
# was generated on macOS (darwin-arm64).


# Always install optional native dependencies
include=optional
